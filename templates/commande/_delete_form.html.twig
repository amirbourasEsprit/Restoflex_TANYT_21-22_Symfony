<form method="post" action="{{ path('app_commande_delete', {'idCmd': commande.idCmd}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande.idCmd) }}">
    <button class="btn btn-danger">Delete</button>
</form>
