<form method="post" action="{{ path('app_produit_restaurant_delete', {'idPdtrest': produit_restaurant.idPdtrest}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit_restaurant.idPdtrest) }}">
    <button class="btn">Delete</button>
</form>
